# Docker Compose only. Replace the placeholder before starting PostgreSQL.
# Keep DB credentials in sync with apps/commerce/.env.
DB_NAME=vendure
DB_USERNAME=vendure
DB_PASSWORD=REPLACE_WITH_LOCAL_DATABASE_PASSWORD
POSTGRES_HOST_PORT=6543
